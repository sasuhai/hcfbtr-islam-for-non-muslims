rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY OPEN RULES FOR DATA IMPORT
    // TODO: Replace with secure rules after import is complete!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
